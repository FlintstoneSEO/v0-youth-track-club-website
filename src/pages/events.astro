---
import { getEntry } from "astro:content";
import PageRenderer from "@/components/PageRenderer.astro";

const page = await getEntry("pages", "events");
if (!page) throw new Error("Missing pages/events.md");
---

<PageRenderer page={page} />
