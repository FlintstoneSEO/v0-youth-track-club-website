---
import { getEntry } from "astro:content";
import PageRenderer from "@/components/PageRenderer.astro";

const page = await getEntry("pages", "faq");
if (!page) throw new Error("Missing pages/faq.md");
---

<PageRenderer page={page} />
