---
import { getEntry } from "astro:content";
import PageRenderer from "@/components/PageRenderer.astro";

const page = await getEntry("pages", "index");
if (!page) throw new Error("Missing pages/index.md");
---

<PageRenderer page={page} />
